// Re-exported from shared/ so the client and the server's bot-prerender
// routes use the exact same per-page title/description data.
export { getPageSeo } from '@shared/pageSeo';
export type { Lang, PageSeo, PageKey } from '@shared/pageSeo';
